// Clipboard module — stubs, will be implemented in later tasks

pub mod monitor {
    use tauri::AppHandle;

    pub fn start(app_handle: AppHandle) {
        todo!("clipboard monitor start")
    }
}
